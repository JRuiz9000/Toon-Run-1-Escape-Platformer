using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LiveCounter : MonoBehaviour
{
    public static int numLives = 3; //Sets life counter to 3

    //public static int numLevel = 1; //Sets the start level

    //public static bool isWinLevel = false; //Boolean variable set to false by default, condition if Win is not met yet, thus in game

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
       
    }
   /* public static void reset()
    {
        //Function to reset values if game is restarted
        numLives = 3;
        //numLevel = 1;

        //isWinLevel = false;
        
    }*/
}
